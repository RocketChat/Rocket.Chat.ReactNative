// export const A11yFocusContext = createContext({} as IA11yFocusContext);

// We need to create the parentTag because using accessibilityElements property
// of a parent UIView we can define the order that the voiceOver will navigate through the elements

// Code structure ideas
// create almost the same structure about it but with a 'readable' code;
