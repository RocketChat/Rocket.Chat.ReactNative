// iOS uses native migration in MMKVMigration.mm
// This is a no-op placeholder for platform-specific imports

const migrateFromOldMMKV = () => {
	console.log('iOS MMKV migration handled natively in MMKVMigration.mm');
};

export default migrateFromOldMMKV;
