import { StyleSheet } from 'react-native';

export const separatorStyles = StyleSheet.create({
	container: {
		flexDirection: 'row',
		alignItems: 'center',
		margin: 12
	},
	line: {
		height: 1,
		flex: 1
	}
});
