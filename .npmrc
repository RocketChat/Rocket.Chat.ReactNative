# Hoisted layout: keep node_modules flat so React Native autolinking,
# Podfile, and Gradle hard-coded `../node_modules/<pkg>` paths resolve
# without changes. Strict pnpm would put every package under
# node_modules/.pnpm/<pkg>@<ver>/node_modules/<pkg> and break those.
node-linker=hoisted
