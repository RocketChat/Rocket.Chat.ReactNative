# generated by react-native-owl
diff/
latest/
report/

# cloned baseline reference repo (source of the action-sheet prints)
reference/

# Baselines are NOT committed here — the reference repo
# (OtavioStasiak/Rocket.Chat.ReactNative.VisualRegressionTest) is the source of
# truth. `scripts/owl-compare.sh` pulls the prints and stages them into
# baseline/<platform>/ at compare time. `baseline/ActionSheet/` is a local
# offline-fallback cache.
baseline/
