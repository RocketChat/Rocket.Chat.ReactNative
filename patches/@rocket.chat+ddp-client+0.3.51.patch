--- a/node_modules/@rocket.chat/ddp-client/dist/Connection.d.ts
+++ b/node_modules/@rocket.chat/ddp-client/dist/Connection.d.ts
@@ -21,6 +21,19 @@
     connect(): Promise<boolean>;
     reconnect(): Promise<boolean>;
     close(): void;
+    /**
+     * Active liveness check: sends a raw DDP `ping` and resolves true if `pong`
+     * arrives within `timeoutMs`. Useful for detecting zombie sockets that
+     * remain in `status === 'connected'` but whose underlying transport is dead
+     * (e.g. mobile NAT timeouts that don't fire `onclose`).
+     */
+    probe(timeoutMs?: number): Promise<boolean>;
+    /**
+     * Tear down the current socket and reconnect from scratch. Concurrent
+     * callers share one in-flight promise so parallel calls don't race each
+     * other. Resolves true once the new socket is connected; false on failure.
+     */
+    forceReopen(): Promise<boolean>;
 }
 interface WebSocketConstructor {
     new (url: string | URL, protocols?: string | string[]): WebSocket;
@@ -43,11 +56,14 @@
     ws: WebSocket | undefined;
     retryCount: number;
     queue: Set<string>;
+    private reopenInFlight;
     constructor(url: string, WS: WebSocketConstructor, client: DDPClient, retryOptions?: RetryOptions);
     private emitStatus;
     reconnect(): Promise<boolean>;
     connect(): Promise<boolean>;
     close(): void;
+    probe(timeoutMs?: number): Promise<boolean>;
+    forceReopen(): Promise<boolean>;
     static create(url: string, webSocketImpl: WebSocketConstructor, client: DDPClient, retryOptions?: RetryOptions): ConnectionImpl;
 }
 export {};
--- a/node_modules/@rocket.chat/ddp-client/dist/Connection.js
+++ b/node_modules/@rocket.chat/ddp-client/dist/Connection.js
@@ -11,6 +11,7 @@
         this.status = 'idle';
         this.retryCount = 0;
         this.queue = new Set();
+        this.reopenInFlight = null;
         this.ssl = url.startsWith('https') || url.startsWith('wss');
         this.url = url.replace(/^https?:\/\//, '').replace(/^wss?:\/\//, '');
         this.client.onDispatchMessage((message) => {
@@ -114,6 +115,50 @@
         (_a = this.ws) === null || _a === void 0 ? void 0 : _a.close();
         this.emitStatus();
     }
+    probe(timeoutMs = 2000) {
+        return new Promise((resolve) => {
+            const ddp = this.client;
+            if (!ddp || typeof ddp.ping !== 'function' || typeof ddp.on !== 'function') {
+                resolve(false);
+                return;
+            }
+            let settled = false;
+            let off;
+            const finish = (alive) => {
+                if (settled) return;
+                settled = true;
+                clearTimeout(timer);
+                if (off) off();
+                resolve(alive);
+            };
+            const timer = setTimeout(() => finish(false), timeoutMs);
+            try {
+                off = ddp.on('pong', () => finish(true));
+                ddp.ping();
+            }
+            catch (_a) {
+                finish(false);
+            }
+        });
+    }
+    forceReopen() {
+        if (this.reopenInFlight) return this.reopenInFlight;
+        try {
+            this.close();
+        }
+        catch (_a) {
+            // ignore — we're rebuilding anyway
+        }
+        const p = this.reconnect()
+            .then((v) => Boolean(v))
+            .catch(() => false);
+        this.reopenInFlight = p;
+        const clear = () => {
+            if (this.reopenInFlight === p) this.reopenInFlight = null;
+        };
+        p.then(clear, clear);
+        return p;
+    }
     static create(url, webSocketImpl, client, retryOptions = { retryCount: 0, retryTime: 1000 }) {
         return new ConnectionImpl(url, webSocketImpl, client, retryOptions);
     }
