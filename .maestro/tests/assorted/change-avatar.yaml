appId: chat.rocket.reactnative
name: Change Avatar
onFlowStart:
  - runFlow: '../../helpers/setup.yaml'
onFlowComplete:
  - evalScript: ${output.utils.deleteCreatedUsers()}
tags:
  - test-5

---
- evalScript: ${output.user = output.utils.createUser()}
- evalScript: ${output.loggedInUser = output.utils.login(output.user.username, output.user.password)}

- runFlow:
    file: '../../helpers/login-with-deeplink.yaml'
    env:
      USERNAME: ${output.user.username}
      PASSWORD: ${output.user.password}
- extendedWaitUntil:
    visible:
      id: 'rooms-list-view-sidebar'
    timeout: 
- tapOn:
    id: 'rooms-list-view-sidebar'
- extendedWaitUntil:
    visible:
      id: 'sidebar-view'
    timeout: 
- extendedWaitUntil:
    visible:
      id: 'sidebar-profile'
    timeout: 
- tapOn:
    id: 'sidebar-profile'
- extendedWaitUntil:
    visible:
      id: 'profile-view'
    timeout: 

# should click on the reset avatar button
- extendedWaitUntil:
    visible:
      id: 'avatar-edit-button'
    timeout: 
- tapOn:
    id: 'avatar-edit-button'
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-avatar'
    timeout: 
- extendedWaitUntil:
    visible:
      id: 'reset-avatar-suggestion'
    timeout: 
- tapOn:
    id: 'reset-avatar-suggestion'

# should appear the discard alert when click the back icon
- tapOn:
    id: 'header-back'
- extendedWaitUntil:
    visible:
      text: '.*Discard changes?.*'
    timeout: 
- extendedWaitUntil:
    visible:
      text: '.*Cancel.*'
    timeout: 
- tapOn:
    text: '.*Cancel.*'
- tapOn:
    id: 'header-back'
- extendedWaitUntil:
    visible:
      text: '.*Discard changes?.*'
    timeout: 
- extendedWaitUntil:
    visible:
      text: 'Discard'
    timeout: 
- tapOn:
    text: 'Discard'

# should change the avatar through upload image
- evalScript: ${output.previousUserInfo = output.utils.getProfileInfo(output.loggedInUser.userId)}
- extendedWaitUntil:
    visible:
      id: 'avatar-edit-button'
    timeout: 
- tapOn:
    id: 'avatar-edit-button'
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-submit'
      enabled: false
    timeout: 
- extendedWaitUntil:
    visible:
      text: 'Upload image'
    timeout: 
- tapOn:
    text: 'Upload image'
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-submit'
      enabled: true
    timeout: 
- tapOn:
    id: 'change-avatar-view-submit'
- extendedWaitUntil:
    visible:
      id: 'profile-view'
    timeout: 
- evalScript: ${output.newUserInfo = output.utils.getProfileInfo(output.loggedInUser.userId)}
- assertTrue: ${output.previousUserInfo.avatarETag !== output.newUserInfo.avatarETag}

# should change the avatar taking a photo
- evalScript: ${output.previousUserInfo = output.utils.getProfileInfo(output.loggedInUser.userId)}
- extendedWaitUntil:
    visible:
      id: 'avatar-edit-button'
    timeout: 
- tapOn:
    id: 'avatar-edit-button'
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-submit'
      enabled: false
    timeout: 
- extendedWaitUntil:
    visible:
      text: 'Take a photo'
    timeout: 
- tapOn:
    text: 'Take a photo'
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-submit'
      enabled: true
    timeout: 
- tapOn:
    id: 'change-avatar-view-submit'
- extendedWaitUntil:
    visible:
      id: 'profile-view'
    timeout: 
- evalScript: ${output.newUserInfo = output.utils.getProfileInfo(output.loggedInUser.userId)}
- assertTrue: ${output.previousUserInfo.avatarETag !== output.newUserInfo.avatarETag}

# should change avatar from URL
- evalScript: ${output.previousUserInfo = output.utils.getProfileInfo(output.loggedInUser.userId)}
- extendedWaitUntil:
    visible:
      id: 'avatar-edit-button'
    timeout: 
- tapOn:
    id: 'avatar-edit-button'
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-submit'
      enabled: false
    timeout: 
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-avatar-url'
    timeout: 
- tapOn:
    id: 'change-avatar-view-avatar-url'
- inputText: 'https://play-lh.googleusercontent.com/AMPmTxPV498lwVj3HsEJkvoUF6RurRcwYz2xbTCP7XndQdaWBmll4eSCcnwtbehUUw=w480-h960-rw'
- extendedWaitUntil:
    visible:
      text: 'Fetch image from URL'
    timeout: 
- tapOn:
    text: 'Fetch image from URL'
- hideKeyboard
- extendedWaitUntil:
    visible:
      id: 'change-avatar-view-submit'
      enabled: true
    timeout: 
- tapOn:
    id: 'change-avatar-view-submit'
- extendedWaitUntil:
    visible:
      id: 'profile-view'
    timeout: 
- evalScript: ${output.newUserInfo = output.utils.getProfileInfo(output.loggedInUser.userId)}
- assertTrue: ${output.previousUserInfo.avatarETag !== output.newUserInfo.avatarETag}
