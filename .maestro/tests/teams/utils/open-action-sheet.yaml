appId: chat.rocket.reactnative
tags:
  - 'util'

---
- extendedWaitUntil:
    visible:
      id: room-members-view-item-${username}
    timeout: 60000
- tapOn:
    id: room-members-view-item-${username}
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible:
      id: action-sheet
    timeout: 60000
- extendedWaitUntil:
    visible:
      id: action-sheet
    timeout: 60000
