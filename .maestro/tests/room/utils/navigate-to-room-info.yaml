appId: ${APP_ID}
name: Check Server
tags:
  - 'util'

---
- runFlow:
    file: '../../../helpers/search-and-navigate-room.yaml'
    env:
      ROOM: ${ROOM}
- extendedWaitUntil:
    visible:
      id: 'room-view-title-.*'
    timeout: 60000
- tapOn:
    id: 'room-view-title-.*'
- extendedWaitUntil:
    visible:
      id: 'room-actions-view'
    timeout: 60000
- extendedWaitUntil:
    visible:
      id: 'room-actions-info'
    timeout: 60000
- tapOn:
    id: 'room-actions-info'
- extendedWaitUntil:
    visible:
      id: 'room-info-view'
    timeout: 60000
