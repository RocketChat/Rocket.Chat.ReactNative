appId: ${APP_ID}
name: Navigate to room
tags:
  - 'util'

---
- waitForAnimationToEnd
- scrollUntilVisible:
    element:
      id: 'rooms-list-view-item-${ROOM}'
    timeout: 60000
- waitForAnimationToEnd
- tapOn:
    id: 'rooms-list-view-item-${ROOM}'
- extendedWaitUntil:
    visible:
      id: 'room-view-title-${ROOM}'
    timeout: 60000
