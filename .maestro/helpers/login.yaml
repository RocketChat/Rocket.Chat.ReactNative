appId: chat.rocket.reactnative
---
- runFlow: register.yaml
#  IF login then logout
- runFlow:
    when: 
       visible: 
          id: "rooms-list-view-sidebar"
    commands:
        - runFlow: logout.yaml
        - runFlow: navigate-to-workspace.yaml
# IF not login just go back to login 
- tapOn:
      text: "Username or email  . Error: "
- inputText:
       text: ${output.user.username}

- tapOn:
      text: "Password  . Error: "
- inputText:
       text: ${output.user.password}
- tapOn:
      id: "login-view-submit"

- runFlow:
    when:
      visible: "OK"
    commands:
       - tapOn: "OK"