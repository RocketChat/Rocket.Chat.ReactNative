executionPrerequisite: RoomsList visible on open.rocket.chat, logged in as
  diego.mello, drawer closed. Flow opens the `support` public channel
  (non-E2EE), lets RoomView first-mount settle, then navigates back to
  RoomsList. Repeat via flow-execute for A/B first-mount profiling.
steps:
  - tool: gesture-tap
    args:
      udid: emulator-5554
      x: 0.5
      y: 0.359
  - tool: gesture-tap
    args:
      udid: emulator-5554
      x: 0.053
      y: 0.071
    delayMs: 3000
