# Throwaway — e2e selection observation

Scenario C5: docs-only change outside `app/` source roots. Expect confident-zero
skip (`should_run=false`), the whole e2e stage skipped, `E2E Result` green.

This file and its PR are deleted after observation.
